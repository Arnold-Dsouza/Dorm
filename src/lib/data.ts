
// Legacy re-exports kept for compatibility while dorm-specific data lives in src/dorms.
export { tabuBuildings as initialBuildingsData, tabuBar as initialBarData, tabuCafeteria as initialCafeteriaData, tabuFitnessRoom as initialFitnessRoomData, tabuPropertyManagement as initialPropertyManagementData, tabuTeaRoom as initialTeaRoomData } from '@/dorms/tabu/data';
